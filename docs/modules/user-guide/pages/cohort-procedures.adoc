:description: The procedure for creating cohorts 

To define a cohort in asclepias,
one must create a `CohortSpec` type.
The `CohortSpec` type takes three inputs:

* The shape of the input data
* The shape of the output data
* an IndexSet

See below for details on creating these types:

== a. Defining Input Data Shape
*TODO this is actually pre-defined in step 1 correct?*

== b. Defining Output Data Shape
*TODO does this get defined in event-data-model as well?*

== c. Defining an IndexSet
include::index-set-procedures.adoc[leveloffset+=1]

== d. Define Tags

*TODO in the asclepias-project-example we needed to do this,*
*but I feel like if we use event-data-model, the tags are already defined*
*in a "real" project, will tags need to be defined in code?*

include::tags-procedures.adoc[leveloffset+=1]

== e. Define Criteria
include::criteria-procedures.adoc[leveloffset=+1]

== f. Create Features
include::features-procedures.adoc[leveloffset=+1]

== g. Define Cohort Specification


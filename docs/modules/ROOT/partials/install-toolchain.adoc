:description: how to install toolchain needed for asclepias
:ghc: 8.10.7

[TIP]
For a more detailed installation instructions with detailed explanations,
xref:dev-guide:page$installing-ghc-and-cabal.adoc[refer the toolchain section]
in the 
xref:dev-guide:page$index.adoc[developer guide].

[NOTE]
If using an M1 mac, 
xref:dev-guide:m1-mac.adoc[see additional instructions].

// install ghcup
include::dev-guide:partial$install-ghcup.adoc[leveloffset=+1]

== Installing the Haskell toolchain

Once `ghcup` is installed,
run the following commands:

[source,shell,subs="attributes"]
----
ghcup install ghc {ghc}
ghcup install cabal
ghcup install hls
----

